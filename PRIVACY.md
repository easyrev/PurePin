# PurePin 隐私与磁盘使用

PurePin 的截图、标注和置顶在本机完成。应用没有内置分析 SDK、账户、上传服务或自动崩溃上报。应用沙箱未申请网络访问 entitlement。只有你点击帮助入口时，应用才会让系统浏览器打开 GitHub；浏览器与 GitHub 的数据处理由各自的隐私政策管理。

## 应用会保存什么

- **偏好设置**：快捷键、是否包含光标、各标注工具的样式，保存在 macOS 管理的偏好设置中。
- **你主动保存的图片**：仅在选择 Save 并确认路径后写入该位置。文件由你管理，应用不会自动删除。
- **你主动复制的内容**：复制截图、Pin 图片或 Support Info 时写入系统剪贴板，替换其原有内容。剪贴板可能由系统通用剪贴板或你安装的其他剪贴板工具处理。
- **系统日志和崩溃报告**：少量操作边界和错误事件使用 macOS 统一日志。macOS 也可能生成崩溃报告，保留期限和占用由系统管理。PurePin 没有自建持续追加的日志文件，不承诺系统日志完全不占磁盘。

PurePin 不创建截图历史库、不自动保存 Pin、不缓存截图到自建磁盘目录，也不周期性扫描崩溃目录。屏幕像素、标注文字、剪贴板内容不会被应用主动写入日志。可能包含文件路径的错误描述按私密字段处理。

这里描述的是普通发布版。维护者的独立基准构建会在明确运行基准场景时生成测量文件；这些入口不包含在普通 Release 产品中。

## 反馈完全由你决定

菜单栏 → **Help & Feedback… → Copy Support Info** 只复制：应用版本、构建号、源码版本的短标识、构建状态、macOS 版本和当前进程架构。它不读取现有剪贴板，不复制截图，不搜集日志、设备序列号、用户名或文件路径，也不自动发送数据。

如需分享系统崩溃报告，请先在 Console 中查看内容，检查用户路径、文件名及其他个人信息。不要将未经检查的报告直接贴到公开 Issues。详见 [反馈指南](SUPPORT.md)。

## 权限

- 截图需要 **Screen & System Audio Recording** 设置中的屏幕录制授权；应用使用静态截图接口，不录制音频。
- 保存图片只使用你通过系统保存面板选定的位置。
- Launch at Login 是可选设置，默认不开启；由系统登录项机制管理。
- 不要求 Accessibility、Camera 或 Microphone 权限。

## 卸载

退出 PurePin，关闭其 Launch at Login 设置，然后移除应用即可。手动保存的图片由你决定是否保留。系统偏好设置、统一日志和系统生成的崩溃报告可能仍然存在；应用不会在卸载后运行清理任务。
